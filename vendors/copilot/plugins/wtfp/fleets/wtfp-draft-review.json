{
  "schema": "wtfp.fleet/v1",
  "id": "wtfp-draft-review",
  "description": "Draft one approved section and independently review its argument and evidence.",
  "parameters": [
    {
      "id": "section",
      "required": true
    }
  ],
  "steps": [
    {
      "id": "draft",
      "role": "role://section-writer",
      "operation": "mutate",
      "writes": [
        "project://paper/{artifact}",
        "project://sections/{section}/summary"
      ],
      "dependsOn": []
    },
    {
      "id": "review",
      "role": "role://section-reviewer",
      "operation": "verify",
      "writes": [],
      "dependsOn": [
        "draft"
      ]
    }
  ],
  "maxConcurrency": 1,
  "failurePolicy": "stop",
  "instructionTemplate": "Draft section {section} from its explicitly approved plan into its declared paper artifact and update only its declared portable Markdown summary. The invoking action orchestrator owns validation, checkpoint, manifest, section, and state reconciliation. The reviewer must independently review the resulting prose against the plan, sources, decisions, and venue constraints without claiming a mutation."
}
