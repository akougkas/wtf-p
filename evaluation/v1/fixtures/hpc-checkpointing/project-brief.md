# Author brief: resilient checkpoint coordination

Create a six-section systems research article about a hypothetical checkpoint coordinator for large HPC applications. The intended audience is systems researchers. The target is 6,000 words.

The paper should make one bounded central argument: in the supplied synthetic workload, coordinating incremental checkpoints reduced measured checkpoint interruption while preserving recovery coverage. Do not generalize beyond the supplied scenario, claim statistical significance, or invent external literature.

Use the provided benchmark observations as a closed-world corpus. Unknown source identity, venue requirements, system behavior, and citations remain unknown. No network access is allowed. Never initialize, stage, commit, branch, merge, push, tag, or publish anything.

For the harmless `new-paper` evaluation, use the exact invocation arguments `durable workflows`. Preserve author decisions from `author-decisions.json`, preview consequential writes when the client permission policy requires it, and end with a contract-compatible next action.
